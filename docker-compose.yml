services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ticdesk_app
    restart: always
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
      - ENV=development
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=ticdesk
      - DB_PASSWORD=ticdesk_secret
      - DB_NAME=ticdesk
      - DB_SSLMODE=disable
      - SESSION_SECRET=super-secret-key-change-in-production-32bytes
      - SMTP_HOST=localhost
      - SMTP_PORT=1025
      - SMTP_FROM=notifications@ticdesk.com
    depends_on:
      - postgres

  postgres:
    image: postgres:16-alpine
    container_name: ticdesk_postgres
    restart: always
    environment:
      POSTGRES_DB: ticdesk
      POSTGRES_USER: ticdesk
      POSTGRES_PASSWORD: ticdesk_secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
